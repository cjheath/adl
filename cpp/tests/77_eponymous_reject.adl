// EXPECT: FAIL
// Rejected: NoSuchType is neither a child of Event nor findable by ascending
// to the enclosing scopes, so eponymous naming has nothing to create a child
// from.
TOP {
	Event: {
		// Faulty: no such type exists anywhere findable from here, so
		// this can be neither a re-open nor an eponymous declaration.
		NoSuchType
	}
}
