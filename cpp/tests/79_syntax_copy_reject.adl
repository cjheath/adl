// EXPECT: FAIL
// Rejected: a Syntax copy ("Syntax = NoSuchType;") needs an existing object to
// copy the Syntax from.
TOP {
	// Faulty: no such object exists anywhere findable from here, so
	// there's nothing whose Syntax could be copied.
	Widget: {
		Syntax = NoSuchType;
	}
}
