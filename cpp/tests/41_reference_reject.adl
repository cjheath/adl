// EXPECT: FAIL
// Rejected: a final Reference restriction inherited from Employment may only
// be narrowed to the declared target or a subtype of it, and Dog is neither.
TOP {
	Company:;
	Dog:;

	Employment: {
		Employer -> Company;		// Final restriction: Employer must be a Company
	}

	// Faulty: Dog is not a Company, nor a subtype of it, so this violates
	// Employer's final restriction inherited from Employment.
	BadJob: Employment {
		Employer = Dog;
	}
}
